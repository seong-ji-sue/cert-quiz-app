# 2026-03-08 — 기존 코드 분석, 라이브러리 설치, 설계 문서 생성 및 프로젝트 설정 정비

## [작업 내용]

### 1. 전수 조사 수행
- 모노레포 5개 패키지 전체 파일 분석 (host, remote, server, client-common, server-common)
- 기술 부채 목록 작성 → `docs/ARCH.md` 반영
- 패키지 의존성 및 버전 호환성 검토

### 2. 오류 수정 (Bug Fixes)

#### 2.1 파일명 공백 버그 — 즉시 수정
- **문제**: `packages/host/src/pages/auth/authorized/ authorized.module.scss` 파일명에 선행 공백 포함
- **원인**: 파일 생성 시 오타로 인한 공백 삽입
- **해결**: `mv " authorized.module.scss" "authorized.module.scss"` 로 파일명 정정
- **영향**: `packages/host/src/pages/auth/authorized/index.js`의 import 경로도 동시 수정
  - Before: `import styles from './ authorized.module.scss';`
  - After: `import styles from './authorized.module.scss';`

#### 2.2 존재하지 않는 패키지 import — 즉시 수정
- **문제**: `packages/server/index.js`에서 `@nextpr/common` import
- **원인**: `@nextpr/common` 패키지는 모노레포 내 존재하지 않음 (실제 패키지는 `@nextpr/server-common`)
- **해결**:
  - `packages/server/index.js`: 불필요한 `testFunc` import 및 호출 제거, 포트를 환경변수(`NODE_SERVER_PORT`) 기반으로 변경
  - `packages/server/package.json`: `@nextpr/common` → `@nextpr/server-common` 의존성 수정

#### 2.3 notify.sh 환경변수 로딩 오류 — 즉시 수정
- **문제**: `notify.sh`가 `.env` 파일만 로드 시도 → 프로젝트에는 `.env.development`만 존재
- **해결**: `.env` 미존재 시 `.env.development`를 fallback으로 소싱하도록 수정

#### 2.4 engines 필드 미설정
- **문제**: 루트 `package.json`에 Node.js/Yarn 버전 제약 없음
- **해결**: `"engines": { "node": ">=20.0.0", "yarn": ">=1.22.0" }` 추가

### 3. 의존성 설치 내역

#### 설치 환경
- Node.js: v20.20.1
- npm: 11.11.0
- Yarn: 미설치 (npm workspace로 대체)

#### 설치 결과
```
npm install 실행 결과:
- 137 packages added, 31 removed, 113 changed
- 총 1,287 packages audited
- 취약점: 45건 (critical 1, high 27, moderate 13, low 4)
```

#### 설치된 핵심 패키지 버전
| 패키지 | 버전 | 상태 |
|--------|------|------|
| next | 14.2.6 | ✅ 설치됨 |
| react | 18.3.1 | ✅ 설치됨 |
| recoil | 0.7.7 | ✅ 설치됨 |
| @module-federation/nextjs-mf | 8.8.11 | ✅ 설치됨 |
| NestJS | - | ❌ 미설치 (Phase 1 작업 예정) |
| Prisma | - | ❌ 미설치 (Phase 1 작업 예정) |

### 4. 설계 문서 생성

| 파일 | 내용 |
|------|------|
| `docs/PLAN.md` | 자격증 AI 학습 앱 기능 기획안 (PDF 업로드, AI 문제 추출, 퀴즈, 채점, 이력) |
| `docs/ARCH.md` | 모노레포 구조 분석 및 NestJS 배치 설계 |
| `docs/SCHEMA.md` | MariaDB/Prisma 스키마 설계 (User, Certificate, Question, QuizAttempt 등) |

### 5. 메모리 저장
- `/root/.claude/projects/-workspace-cert-quiz-app/memory/MEMORY.md` 생성

---

## [오류 기록]

### ERROR-001: @nextpr/common not found
- **에러**: `import {testFunc} from '@nextpr/common'` — 패키지 없음
- **원인**: 초기 개발 시 존재하던 패키지 이름이 변경되거나 삭제됨
- **해결**: 해당 import 및 사용 코드 제거, 포트 설정을 환경변수 기반으로 변경

### ERROR-002: 파일명 공백
- **에러**: Next.js가 ` authorized.module.scss` 파일을 정상 해석 불가 (컴파일 오류)
- **해결**: 파일명 rename + import 경로 수정

### ERROR-003: npm 취약점 45건
- **내용**: eslint 8.x 지원 종료, babel-eslint deprecated, 기타 패키지 보안 이슈
- **해결 방안**: `npm audit fix` 적용 필요 (breaking changes 포함 이슈는 수동 검토)
- **현재 상태**: 미결 (개발 단계에서는 영향 낮음, 프로덕션 전 반드시 해결)

---

## [결과 확인]

- [x] 파일명 공백 제거 완료, import 경로 수정 완료
- [x] server/index.js 실행 가능 코드로 수정 완료
- [x] package.json 의존성 오류 수정 완료
- [x] notify.sh 환경변수 로딩 오류 수정 완료
- [x] npm install 성공 (1,287 packages)
- [x] docs/PLAN.md, ARCH.md, SCHEMA.md, history/2026-03-08.md 생성 완료
- [ ] NestJS 백엔드 패키지 생성 (Phase 1 다음 단계)
- [ ] Prisma 스키마 마이그레이션 (Phase 1 다음 단계)

---

## [추가 작업 — feature/project-config-update]

### 5. 사용자 피드백 반영 (프로젝트 규칙 정비)

#### 5.1 패키지 매니저 yarn으로 통일
- Yarn 4.13.0 (corepack) 설치 및 활성화
- 루트 `package.json`에 `"packageManager": "yarn@4.13.0"` 명시
- `"engines": { "yarn": ">=4.0.0" }` 로 버전 제약 강화
- yarn 4 node-modules linker (`.yarnrc.yml`) 확인 — 기존 설정 유지

#### 5.2 peer dependency 경고 수정
- `@nextpr/client-common`: `react-toastify` peerDep `^10.0.5` → `^11.0.0` 으로 수정
- `@nextpr/remote`: `axios` 직접 의존성 추가 (`@nextpr/server-common` peerDep 충족)
- 루트 `package.json`: `typescript`, `webpack` devDependencies 추가 (eslint-config-next, dotenv-webpack 요구사항)

#### 5.3 .env 파일 전략 수정
- `.gitignore`에서 `.env.development`, `.env.production` 제거 (git에 추적)
- `.env.local`, `.env.*.local`은 gitignore 유지 (실제 시크릿 보관용)
- `.env.development` 내 Slack Webhook URL 실제값 → placeholder로 sanitize
- GitHub Push Protection 대응 완료

#### 5.4 CLAUDE.md 대폭 업데이트
- yarn 4 사용 명시, packages 구조 설명 추가
- .env 2개 파일 전략 설명
- 작업별 브랜치 생성 원칙 추가
- CLI Commands 업데이트

#### 5.5 docs/ARCH.md 업데이트
- `packages/api` (NestJS) 예정 구조 반영
- 환경변수 관리 전략 테이블 추가
- 기술 부채 목록 현행화

#### 5.6 README.md 신규 생성 (CLAUDE.md와 동일 위치)
- 프로젝트 개요, 설치 방법, 기술 스택, 문서 링크, Git 전략 포함
- GitHub 저장소에 공개 노출되는 진입점 문서

### 6. 의존성 재설치 (yarn)
```
yarn install 결과:
- 1,171 packages added (463.67 MiB)
- Peer dependency 경고 잔여: eslint-config-next 일부 (개발 환경 영향 낮음)
```

---

## [추가 작업 — feature/workflow-setup]

### 7. 개발 워크플로우 확립

#### 7.1 신규 문서 생성
- `docs/README.md`: docs/ 내 모든 .md 파일 목록 및 설명 (갱신 시점 포함)
- `docs/WORKFLOW.md`: 전체 개발 프로세스 (브랜치→커밋→리뷰→검증→Slack→머지)
- `docs/review/`: AI 코드 리뷰 기록 디렉터리 신설

#### 7.2 스크립트 신규 생성 (scripts/)
- `scripts/verify.sh`: 서버 실행 및 curl 헬스체크 자동화
- `scripts/ship.sh`: 푸시 + 검증 + Slack 알림 통합 실행
- `scripts/review.sh`: AI 리뷰 결과 MD 파일로 기록

#### 7.3 notify.sh 개선
- 브랜치명, 날짜, PR URL, 검증 URL 포함한 풍부한 알림 포맷
- JSON 인젝션 취약점 수정 (printf 방식 적용)
- SLACK_WEBHOOK_URL 미설정 시 명확한 오류 메시지

#### 7.4 CLAUDE.md 전면 업데이트
- 워크플로우 6단계 프로세스 명시
- 자율 실행 원칙 (승인 불필요/필요 항목 명확히 구분)
- 문서 파일 설명 테이블 추가
- CLI Commands 업데이트

#### 7.5 AI 리뷰 수행
- 리뷰 파일: `docs/review/2026-03-08-feature-workflow-setup.md`
- 판정: ✅ 머지 가능 (JSON 인젝션 수정 후 이상 없음)

---

## [추가 작업 — feature/init-architecture]

### 8. 에이전틱 엔지니어링 아키텍처 완성

#### 8.1 도구 설치
- lazygit v0.59.0 (arm64/x86_64): 설치 완료
- jq: 설치 완료 (JSON 처리)
- bat: 설치 완료 (코드 하이라이팅)
- glow: arm64 미지원 → bat 대체

#### 8.2 Executable Markdown 도입
- `scripts/claude-run.sh`: Python3 기반 마크다운 코드 블록 추출/실행기
- shebang(`#!/usr/bin/env claude-run`) 추가 파일: PLAN.md, ARCH.md, WORKFLOW.md, SCHEMA.md
- `<!-- run 레이블 -->` 태그로 선택 실행 지원

#### 8.3 ComCBT 데이터 파이프라인 설계
- comcbt.com 구조 실제 분석: XE CMS, SID 세션 토큰 기반
- 확인된 카테고리: 정보처리기사(/xe/iz), 컴퓨터활용능력 1·2급, 네트워크관리사 2급
- 시험지 URL 패턴: `/xe/{category}/{document_srl}` (예: /xe/iz/5851061)
- 다운로드 방식: `procFileDownload&file_srl=...&sid=...` (Playwright 세션 필요)
- `data/registry.json`, `data/checksums.json`, `data/parsed/`, `data/changelog/` 구조 초기화
- `scripts/sync-data.sh`: 주간 자동 동기화 파이프라인 설계

#### 8.4 다중 모델 오케스트레이션 (ARCH.md)
- Claude: 핵심 AI (해설, 코드 리뷰), Gemini: UI/UX + OCR, Grok: 트렌드 분석
- `.env.example`에 ANTHROPIC_API_KEY, GOOGLE_API_KEY, XAI_API_KEY 추가
- 비용 관리 전략: 캐싱, 배치 처리, 모델 선택, 예산 알림 설계

#### 8.5 AI 리뷰어 검토
- 리뷰 파일: `docs/review/2026-03-08-feature-init-architecture.md`
- 발견 오류: ARCH.md grep exit 1 버그 → 즉시 수정
- 판정: ✅ 머지 가능

#### 8.6 오류 기록
- **ERROR-004**: `glow` arm64 미지원 → `bat` 대체 (기능 동일)
- **ERROR-005**: ARCH.md grep 0건 시 exit 1 → `|| echo 0` 패턴 수정
- **ERROR-006**: registry.json jq 파이프 오류 → Python json.dumps 방식 전환
